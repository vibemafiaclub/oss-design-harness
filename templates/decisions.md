<!--
B단계(발산·수렴) 산출물 템플릿. 프로젝트 폴더로 복사해서 채운다.
빈 템플릿입니다 — 아래는 전부 TODO/형식 예시이며 실제 내용이 아닙니다.

★ 이 파일은 append 전용이다. 새 라운드는 아래에 덧붙이고, 이전 라운드를 덮어쓰지 않는다.
  이전 라운드를 지우면 라우팅 3(반복 실패) 판정 근거와 재시도 카운터가 동시에 사라진다.
★ 이 파일은 C단계 검증 Agent에게 주지 않는다 (제작 의도를 알면 자기변호를 한다).
-->

# Decisions

## 라운드 로그

> 라우팅 판정이 이 표를 읽는다. **"C단계 탈락 항목" 열을 라운드 간 비교해** 반복 실패를 판정하고,
> 라운드 수가 그대로 재시도 카운터가 된다(기본 상한 3). 비워 두면 무한루프 방지가 작동하지 않는다.

| 라운드 | 시작 사유 | C단계 탈락 항목 | 라우팅 판정 |
|---|---|---|---|
| 1 | 최초 발산 | TODO | TODO |

---

## 라운드 1

### 시작 사유

TODO: `최초 발산` — 또는 재발산이면 `라운드 N에서 [항목] 탈락 → 방향 오류 판정으로 회귀`

### 식별된 독립 축

> 축을 나누지 않고 화면 전체를 통째로 비교하면, 상대가 무엇 때문에 골랐는지 뒤섞여 판단 근거가 안 남는다.
> **독립성 검사**: 한 축의 값을 바꿔도 다른 축이 따라 바뀌지 않아야 한다. 따라 바뀌면 한 축이다 — 합친다.

- TODO: 축 1
- TODO: 축 2

### 축별 후보·비평·선택

> 후보는 서로를 모르는 Agent가 병렬로 만든다. 비평은 서로의 평가를 모르는 페르소나 Agent가 낸다.
> 후보를 만든 Agent가 그 후보를 비평하지 않는다.

#### 축: TODO

| 후보 | 설명 | 페르소나 A 비평 | 페르소나 B 비평 | 선택 |
|---|---|---|---|---|
| 1 | TODO | TODO | TODO | |
| 2 | TODO | TODO | TODO | |

**선택 이유**: TODO

**남은 트레이드오프**: TODO

### C단계 결과

- **탈락 항목**: TODO (없으면 `없음 — 통과`)
- **라우팅 판정**: TODO (`1 국소 결함` / `2 방향 오류` / `3 반복 실패` / `해당 없음`)
- **다음 행동**: TODO

---

<!--
## 라운드 2

새 라운드는 위를 지우지 않고 여기에 라운드 1과 같은 형식으로 덧붙인다.
라운드 3 도달 시 사람에게 에스컬레이션하고 멈춘다 — 에이전트가 상한을 스스로 늘리지 않는다.
-->

## 인수인계 메모

> 드라이버 스위치 시 이어 쓴다. 별도 파일을 만들지 않는다.

- TODO: 누가 → 누구로, 어디까지 했고 다음은 무엇인지
