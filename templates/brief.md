<!--
0단계(요구사항 정렬) 산출물 템플릿. 프로젝트 폴더로 복사해서 채운다.
빈 템플릿입니다 — 아래는 전부 TODO/형식 예시이며 실제 내용이 아닙니다.

이 파일은 두 곳에서 기계적으로 읽힌다:
  - A단계가 "A단계 기준값" 블록의 키를 읽어 PASS/FAIL을 판정한다 (키 이름을 바꾸면 A단계가 못 찾는다)
  - C단계 검증 Agent가 "역추출한 판단기준" 섹션만 받는다 (decisions.md는 주지 않는다)
-->

# Brief

## 보여준 레퍼런스 + 반응 원문

★ **반응은 원문 그대로 적는다. 요약하지 않는다.** 요약하면 역추출의 근거가 사라진다 —
"산만하다"와 "정보가 많아 부담된다"는 서로 다른 기준을 가리킨다.

| # | 레퍼런스 (링크/스크린샷) | 반응 원문 | 좋다/싫다 |
|---|---|---|---|
| 1 | TODO | "TODO — 들은 말 그대로" | |
| 2 | TODO | "TODO" | |

(형식만 참고. 실제 프로젝트에서는 레퍼런스 목록 자체를 새로 만든다.)

## 역추출한 암묵적 판단기준

> C단계 검증 Agent에게 주는 섹션이다. 여기 없는 기준으로 C단계가 탈락시킬 수 없다.

- TODO: 반응에서 뽑아낸 기준 (예: "산만하다" → 정보밀도를 낮게 선호)
- TODO

## 명시적 가정 로그

> 확정 안 된 정보를 침묵 처리하지 않는다. 가정으로 적어 두면 나중에 어디가 틀렸는지 추적된다.

| 항목 | 가정한 내용 | 확인 필요 여부 |
|---|---|---|
| TODO | TODO | |

## A단계 기준값

> A단계가 이 블록의 키를 그대로 읽는다. **키 이름은 바꾸지 말고 값만 채운다.**
> 비워 두면 그 항목은 `SKIP(기준값 없음)`으로 보고되고, 에이전트가 추측해서 판정하지 않는다.

```yaml
tokens:
  color:
    allowed:            # A-1. 허용 색상값/토큰 목록
      - TODO
  type:
    allowed:            # A-2. 허용 텍스트 스타일 목록
      - TODO
  spacing:
    grid: TODO          # A-3. 그리드 단위 (예: 8)

thresholds:
  component_reuse_min: TODO   # A-4. 컴포넌트 재사용률 하한 (예: 0.7)

naming:
  convention: TODO      # A-5. 레이어 네이밍 컨벤션

variants:
  required_states:      # A-6. 필수 배리언트 상태 (존재 여부만 A단계에서 판정)
    - TODO
```

## 인수인계 메모

> 드라이버 스위치 시 이어 쓴다. 별도 파일을 만들지 않는다.

- TODO: 누가 → 누구로, 어디까지 했고 다음은 무엇인지
